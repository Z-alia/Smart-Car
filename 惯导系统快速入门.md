# 惯性导航系统快速入门指南

## 一、5分钟快速上手

### 1. 初始化（在main函数中）

```c
#include "inertial_nav.h"
#include "control_pid.h"

int main(void)
{
    // ... 硬件初始化 ...
    
    // 初始化惯导系统
    inertial_nav_init(
        0.16f,      // 轮距(m)
        0.034f,     // 轮半径(m)
        0.7f        // 陀螺仪信任度(0~1)
    );
    
    // 初始化速度控制器
    cascade_pid_init(
        0.008f,             // 半轮距
        0.0f, 0.0f, 0.0f,   // 图像环PID(不用可设0)
        50.0f, 0.0f, 0.0f   // 速度环PID
    );
    
    // 启动50Hz定时器
    HAL_TIM_Base_Start_IT(&htim6);  // 20ms周期
    
    while(1) {
        // 主循环处理
    }
}
```

### 2. 定时器中断（50Hz, 20ms周期）

```c
void TIM6_DAC_IRQHandler(void)
{
    if (__HAL_TIM_GET_FLAG(&htim6, TIM_FLAG_UPDATE)) {
        __HAL_TIM_CLEAR_FLAG(&htim6, TIM_FLAG_UPDATE);
        
        // 读取IMU
        ICM42688P_Read();
        float gyro_z = icm42688p.gyro_z * 0.0174533f; // deg/s -> rad/s
        
        // 更新惯导（自动进行轨迹记录）
        inertial_nav_update(gyro_z, 
                           control.left_speed, 
                           control.right_speed);
    }
}
```

### 3. 录制轨迹

```c
// 按下按键1 - 开始录制
if (button1_pressed) {
    inertial_nav_start_recording();
    printf("开始录制...\n");
}

// 按下按键2 - 停止录制
if (button2_pressed) {
    uint16_t count = inertial_nav_stop_recording();
    printf("录制完成! %d个航点\n", count);
}
```

### 4. 复现轨迹

```c
// 按下按键3 - 开始复现
if (button3_pressed) {
    uint8_t ret = inertial_nav_start_replay();
    if (ret == 0) {
        printf("开始复现...\n");
    }
}

// 在定时器中断中添加（复现时执行）
void TIM6_DAC_IRQHandler(void)
{
    // ... 惯导更新代码 ...
    
    // 如果在复现模式
    InertialNav_t* nav = inertial_nav_get_state();
    if (nav->trajectory.is_replaying) {
        
        // 获取跟踪控制指令
        float v_cmd, omega_cmd;
        inertial_nav_tracking_control(&v_cmd, &omega_cmd);
        
        // 转换为左右轮速度
        float vL = v_cmd - omega_cmd * 0.08f;  // 半轮距0.08m
        float vR = v_cmd + omega_cmd * 0.08f;
        
        // 速度环PID
        float pwm_L, pwm_R;
        cascade_pid_inner_loop(control.left_speed, 
                              control.right_speed, 
                              &pwm_L, &pwm_R);
        
        // 输出到电机
        motor_output(pwm_L, pwm_R);
    }
}
```

## 二、完整工作流程

```
1. 上电初始化
   ↓
2. 按键1 → 开始录制
   ↓
3. 手动控制小车走轨迹（图像巡线/遥控）
   ↓
4. 按键2 → 停止录制
   ↓
5. 将小车放回起点
   ↓
6. 按键3 → 开始复现
   ↓
7. 小车自动沿轨迹行驶
   ↓
8. 到达终点或按键4停止
```

## 三、调试技巧

### 1. 查看实时位姿

```c
void debug_print_pose(void)
{
    const Pose_t* pose = inertial_nav_get_pose();
    printf("位置: x=%.2fm, y=%.2fm, 航向=%.1f°\n",
           pose->x, pose->y, pose->theta * 57.3f);
}
```

### 2. 查看轨迹信息

```c
void debug_print_trajectory(void)
{
    uint16_t count;
    uint32_t duration;
    float distance;
    
    inertial_nav_get_trajectory_info(&count, &duration, &distance);
    printf("航点数: %d\n", count);
    printf("时长: %.1f秒\n", duration / 1000.0f);
    printf("距离: %.2f米\n", distance);
}
```

### 3. 查看复现精度

```c
void debug_print_errors(void)
{
    float max_pos, avg_pos, max_heading;
    inertial_nav_get_error_stats(&max_pos, &avg_pos, &max_heading);
    
    printf("最大位置误差: %.3f m\n", max_pos);
    printf("平均位置误差: %.3f m\n", avg_pos);
    printf("最大航向误差: %.1f °\n", max_heading * 57.3f);
}
```

## 四、常见问题排查

### 问题1: 录制时位置不更新
**原因**: 定时器中断未正确调用`inertial_nav_update()`
**解决**: 检查定时器配置，确认50Hz中断正常

### 问题2: 复现时不跟踪
**原因**: 未在中断中调用`inertial_nav_tracking_control()`
**解决**: 检查状态判断，确保复现模式下执行控制

### 问题3: 复现精度差
**原因**: PID参数不合适
**解决**: 调整参数
```c
// 位置误差大 -> 增大Kp
inertial_nav_set_position_pid(2.0f, 0.05f, 0.1f);

// 航向误差大 -> 增大Kp
inertial_nav_set_heading_pid(3.0f, 0.1f, 0.2f);
```

### 问题4: 轨迹漂移
**原因**: 陀螺仪漂移或编码器打滑
**解决**: 调整融合系数
```c
// 陀螺仪漂移 -> 降低信任度
inertial_nav_set_gyro_trust(0.5f);

// 编码器打滑 -> 提高信任度
inertial_nav_set_gyro_trust(0.8f);
```

## 五、性能参数参考

| 参数 | 推荐值 | 说明 |
|------|--------|------|
| 录制时长 | ≤60秒 | 最多3000个航点 |
| 采样频率 | 50Hz | 20ms周期 |
| 复现精度 | ±5cm | 典型值 |
| 航向精度 | ±3° | 典型值 |
| 最大速度 | 2m/s | 建议值 |
| 轮距 | 0.1~0.3m | 根据实际 |

## 六、定时器配置示例

```c
// STM32H750 APB1时钟 = 120MHz
// 目标频率 = 50Hz (周期20ms)

void MX_TIM6_Init(void)
{
    htim6.Instance = TIM6;
    htim6.Init.Prescaler = 12000 - 1;  // 120MHz/12000 = 10kHz
    htim6.Init.Period = 200 - 1;        // 10kHz/200 = 50Hz
    htim6.Init.AutoReloadPreload = TIM_AUTORELOAD_PRELOAD_ENABLE;
    
    HAL_TIM_Base_Init(&htim6);
    
    // 使能中断
    HAL_NVIC_SetPriority(TIM6_DAC_IRQn, 2, 0);
    HAL_NVIC_EnableIRQ(TIM6_DAC_IRQn);
}
```

## 七、内存占用

- **RAM**: 24 KB (3000航点 × 8字节)
- **Flash**: 增加约10-15 KB
- **栈**: 约2 KB (中断调用)

## 八、API速查表

| 函数 | 功能 | 调用频率 |
|------|------|----------|
| `inertial_nav_init()` | 初始化 | 1次 |
| `inertial_nav_update()` | 更新位姿 | 50Hz |
| `inertial_nav_start_recording()` | 开始录制 | 按需 |
| `inertial_nav_stop_recording()` | 停止录制 | 按需 |
| `inertial_nav_start_replay()` | 开始复现 | 按需 |
| `inertial_nav_tracking_control()` | 跟踪控制 | 50Hz(复现时) |
| `inertial_nav_get_pose()` | 获取位姿 | 按需 |
| `inertial_nav_get_error_stats()` | 获取误差 | 按需 |

## 九、示例代码下载

完整示例代码见: `惯导系统使用例程.c`

包含:
- ✅ 完整初始化代码
- ✅ 状态机实现
- ✅ 按键处理
- ✅ LCD显示
- ✅ 调试函数
- ✅ 高级应用示例
