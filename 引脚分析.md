# OV2640 + DCMI 引脚分析报告

## 📌 DCMI 数据和控制引脚（从 dcmi.c）

| 功能 | 引脚 | 端口 | 复用功能 | 状态 |
|------|------|------|----------|------|
| HSYNC | PA4 | GPIOA | AF13_DCMI | ✅ 正常（SR=0x0001 检测到） |
| PIXCLK | PA6 | GPIOA | AF13_DCMI | ⚠️ 未知 |
| VSYNC | PB7 | GPIOB | AF13_DCMI | ❌ **疑似问题**（MISR=0x0000） |
| D0 | PC6 | GPIOC | AF13_DCMI | ✅ 应该正常 |
| D1 | PC7 | GPIOC | AF13_DCMI | ✅ 应该正常 |
| D2 | PE0 | GPIOE | AF13_DCMI | ✅ 应该正常 |
| D3 | PE1 | GPIOE | AF13_DCMI | ✅ 应该正常 |
| D4 | PE4 | GPIOE | AF13_DCMI | ✅ 应该正常 |
| D5 | PD3 | GPIOD | AF13_DCMI | ✅ 应该正常 |
| D6 | PE5 | GPIOE | AF13_DCMI | ✅ 应该正常 |
| D7 | PE6 | GPIOE | AF13_DCMI | ✅ 应该正常 |

## 📌 OV2640 控制引脚（从 dcmi_ov2640.h）

| 功能 | 引脚 | 端口 | 类型 | 状态 |
|------|------|------|------|------|
| PWDN | PD14 | GPIOD | 输出 | ✅ 正常（初始化成功） |
| SCCB_SCL | PD6 | GPIOD | 开漏输出 | ✅ 正常（通信成功） |
| SCCB_SDA | PD7 | GPIOD | 开漏输出 | ✅ 正常（通信成功） |

## 🔍 引脚冲突检查

### GPIO 初始化中的其他引脚（从 gpio.c）

| 引脚 | 功能 | 类型 | 潜在冲突 |
|------|------|------|----------|
| PA2 | LED_signal | 输出 | ✅ 无冲突 |
| PA3 | Press | 输入 | ✅ 无冲突 |
| PA5 | LCD_SCL (SPI1) | 复用 | ✅ 无冲突 |
| PA8 | MOTOR_PWM_LEFT | 复用(TIM) | ✅ 无冲突 |
| PA9 | MOTOR_PWM_RIGHT | 复用(TIM) | ✅ 无冲突 |
| PB0 | LCD_DL | 输出 | ✅ 无冲突 |
| **PB7** | **VSYNC (DCMI)** | **AF13** | **⚠️ 检查硬件连接！** |
| PB12 | WIFI_CS | 输出 | ✅ 无冲突 |
| PC4 | LCD_CS | 输出 | ✅ 无冲突 |
| PC5 | LCD_DC | 输出 | ✅ 无冲突 |
| PD8 | PH_LMOTOR | 输出 | ✅ 无冲突 |
| PD9 | PH_RMOTOR | 输出 | ✅ 无冲突 |
| PD14 | OV2640_PWDN | 输出 | ✅ 无冲突 |
| PE7 | wifi_io1 | 输出 | ✅ 无冲突 |
| PE8 | wifi_io2 | 输入 | ✅ 无冲突 |

## 🚨 **关键发现**

### 1. VSYNC 引脚配置（PB7）
- ✅ **软件配置正确**：在 `dcmi.c` 中已配置为 `AF13_DCMI`
- ❌ **硬件可能有问题**：
  - `SR = 0x0001`：只检测到 HSYNC，没有 VSYNC
  - `MISR = 0x0000`：没有帧中断（需要 VSYNC 触发）
  - **可能原因**：
    1. PB7 引脚与 OV2640 的 VSYNC 输出没有连接
    2. OV2640 模块的 VSYNC 引脚虚焊或断线
    3. OV2640 模块本身有问题

### 2. PIXCLK 引脚（PA6）
- 配置为 DCMI_PIXCLK
- **需要验证**：OV2640 是否输出 PCLK 信号

### 3. GPIO 速度问题
- 当前 DCMI 引脚速度：`GPIO_SPEED_FREQ_LOW`
- **建议**：改为 `GPIO_SPEED_FREQ_HIGH` 或 `VERY_HIGH`

## 🎯 诊断步骤

### 第一步：恢复 COM10 寄存器
```c
{0x15, 0x00}  // 恢复默认极性
```

### 第二步：修改 DCMI 引脚速度
在 `dcmi.c` 的 HAL_DCMI_MspInit 中，所有 GPIO 初始化改为：
```c
GPIO_InitStruct.Speed = GPIO_SPEED_FREQ_VERY_HIGH;  // 从 LOW 改为 VERY_HIGH
```

### 第三步：硬件检查清单
1. ⚠️ **检查 PB7 引脚焊接**（VSYNC）
2. ⚠️ **检查 PA6 引脚焊接**（PCLK）
3. ⚠️ **检查 PA4 引脚焊接**（HSYNC）- 虽然检测到了，但也要确认
4. ⚠️ **检查 OV2640 模块电源**（3.3V）
5. ⚠️ **检查 OV2640 模块的晶振**（是否起振）
6. ⚠️ **测量 PCLK 引脚**（用示波器或万用表测频率）

### 第四步：添加硬件诊断代码
在 `main.c` 中添加 GPIO 状态读取：
```c
// 读取 VSYNC 引脚状态
uint8_t vsync_state = HAL_GPIO_ReadPin(GPIOB, GPIO_PIN_7);
// 读取 HSYNC 引脚状态  
uint8_t hsync_state = HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_4);
// 读取 PCLK 引脚状态
uint8_t pclk_state = HAL_GPIO_ReadPin(GPIOA, GPIO_PIN_6);

// 显示在 LCD 上
LCD_ShowString(10, 100, "VSYNC=%d HSYNC=%d PCLK=%d", vsync_state, hsync_state, pclk_state);
```

## 🔧 可能的解决方案优先级

### 方案 1：修改 GPIO 速度（最简单）⭐⭐⭐⭐⭐
修改 `dcmi.c` 中所有 DCMI 引脚速度为 VERY_HIGH

### 方案 2：检查硬件连接（最可能）⭐⭐⭐⭐
用万用表/示波器检查 PB7（VSYNC）和 PA6（PCLK）

### 方案 3：尝试其他 VSYNC 引脚（如果 PB7 有问题）⭐⭐⭐
STM32H7 的 DCMI 可能支持其他引脚作为 VSYNC

### 方案 4：降低帧率要求（临时方案）⭐⭐
修改 OV2640 配置，降低输出帧率，看是否能稳定捕获

## 📊 当前状态总结

| 项目 | 状态 | 说明 |
|------|------|------|
| SCCB 通信 | ✅ 正常 | 可以读取 ID，写入寄存器 |
| OV2640 初始化 | ✅ 成功 | Device ID 读取正确 |
| DCMI 使能 | ✅ 正常 | CR=0x4005，DCMI 已启用 |
| HSYNC 检测 | ✅ 正常 | SR=0x0001，检测到 HSYNC |
| VSYNC 检测 | ❌ **失败** | MISR=0x0000，无帧中断 |
| 帧捕获 | ❌ 异常 | FPS=1，不正常 |

**结论**：VSYNC 信号是问题根源！需要检查 PB7 引脚的硬件连接。
