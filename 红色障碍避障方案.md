# 红色障碍完全出赛道避障方案（不依赖图像）

## 一、方案概述

本方案设计用于智能车在遇到红色障碍时，**完全离开赛道**进行绕行避障，避障过程中**不依赖摄像头图像**，完全依靠编码器和陀螺仪的航位推算（Dead Reckoning）实现精确控制。

---

## 二、硬件依赖

| 传感器 | 作用 | 数据来源 |
|--------|------|----------|
| **编码器** (TIM2/TIM3) | 测量行驶距离 | `encoder_left_count`, `encoder_right_count` |
| **陀螺仪** (ICM42688) | 测量转向角度 | `imu_data.gyro_z` (Z轴角速度) |
| **路径积分器** | 距离累积 | `distance_integral` |
| **角度积分器** | 角度累积 | `angle_integral` |

---

## 三、避障流程设计

### 3.1 避障状态机

```
┌──────────┐
│  ENTER   │ 检测到红色障碍，记录进入位置
└────┬─────┘
     │
     ↓
┌──────────┐
│ PHASE_1  │ 直行一段距离（脱离检测区）
└────┬─────┘
     │
     ↓
┌──────────┐
│ PHASE_2  │ 左转90° + 前进H距离（横向移动）
└────┬─────┘
     │
     ↓
┌──────────┐
│ PHASE_3  │ 右转90° + 前进L距离（绕过障碍）
└────┬─────┘
     │
     ↓
┌──────────┐
│ PHASE_4  │ 右转90° + 前进H距离（回到赛道）
└────┬─────┘
     │
     ↓
┌──────────┐
│ PHASE_5  │ 左转90° + 继续巡线
└────┬─────┘
     │
     ↓
┌──────────┐
│   OUT    │ 完成避障，恢复图像巡线
└──────────┘
```

### 3.2 关键参数定义

```c
#define RED_OBS_FORWARD_DIST     20.0f   // 第一段直行距离(cm)
#define RED_OBS_LATERAL_DIST     30.0f   // 横向移动距离H(cm) - 离开赛道
#define RED_OBS_BYPASS_DIST      40.0f   // 绕行距离L(cm) - 超过障碍物长度
#define RED_OBS_TURN_ANGLE       90.0f   // 转向角度(度)
#define RED_OBS_FORWARD_SPEED    50.0f   // 前进速度(cm/s)
#define RED_OBS_TURN_SPEED       30.0f   // 转向速度(cm/s)
```

---

## 四、核心实现逻辑

### 4.1 状态切换逻辑（伪代码）

```c
typedef enum {
    RED_OBS_IDLE = 0,      // 未进入避障
    RED_OBS_ENTER,         // 检测到障碍
    RED_OBS_PHASE_1,       // 阶段1: 直行脱离检测区
    RED_OBS_PHASE_2,       // 阶段2: 左转90° + 前进H
    RED_OBS_PHASE_3,       // 阶段3: 右转90° + 前进L
    RED_OBS_PHASE_4,       // 阶段4: 右转90° + 前进H
    RED_OBS_PHASE_5,       // 阶段5: 左转90°
    RED_OBS_OUT            // 避障完成
} RedObstacleState;

static RedObstacleState red_obs_state = RED_OBS_IDLE;

void red_obstacle_avoid(void)
{
    switch(red_obs_state)
    {
        case RED_OBS_IDLE:
            // 等待进入信号（由图像检测触发）
            break;
            
        case RED_OBS_ENTER:
            // 记录进入位置，初始化积分器
            clear_distant_integeral();
            clear_angle_integeral();
            red_obs_state = RED_OBS_PHASE_1;
            break;
            
        case RED_OBS_PHASE_1:
            // 直行20cm
            begin_distant_integeral(RED_OBS_FORWARD_DIST);
            motor_set_speed_direct(RED_OBS_FORWARD_SPEED, RED_OBS_FORWARD_SPEED);
            
            if(get_integeral_state(&distance_integral) == 2) {
                clear_distant_integeral();
                red_obs_state = RED_OBS_PHASE_2;
            }
            break;
            
        case RED_OBS_PHASE_2:
            // 左转90° + 前进H(30cm)
            // 子状态: TURN → FORWARD
            static uint8_t phase2_substep = 0;
            
            if(phase2_substep == 0) {
                // 左转90°
                begin_angle_integeral(-RED_OBS_TURN_ANGLE);  // 负值表示左转
                motor_set_speed_direct(-RED_OBS_TURN_SPEED, RED_OBS_TURN_SPEED);  // 原地左转
                
                if(get_integeral_state(&angle_integral) == 2) {
                    clear_angle_integeral();
                    phase2_substep = 1;
                }
            }
            else if(phase2_substep == 1) {
                // 前进30cm
                begin_distant_integeral(RED_OBS_LATERAL_DIST);
                motor_set_speed_direct(RED_OBS_FORWARD_SPEED, RED_OBS_FORWARD_SPEED);
                
                if(get_integeral_state(&distance_integral) == 2) {
                    clear_distant_integeral();
                    phase2_substep = 0;
                    red_obs_state = RED_OBS_PHASE_3;
                }
            }
            break;
            
        case RED_OBS_PHASE_3:
            // 右转90° + 前进L(40cm)
            static uint8_t phase3_substep = 0;
            
            if(phase3_substep == 0) {
                // 右转90°
                begin_angle_integeral(RED_OBS_TURN_ANGLE);  // 正值表示右转
                motor_set_speed_direct(RED_OBS_TURN_SPEED, -RED_OBS_TURN_SPEED);  // 原地右转
                
                if(get_integeral_state(&angle_integral) == 2) {
                    clear_angle_integeral();
                    phase3_substep = 1;
                }
            }
            else if(phase3_substep == 1) {
                // 前进40cm
                begin_distant_integeral(RED_OBS_BYPASS_DIST);
                motor_set_speed_direct(RED_OBS_FORWARD_SPEED, RED_OBS_FORWARD_SPEED);
                
                if(get_integeral_state(&distance_integral) == 2) {
                    clear_distant_integeral();
                    phase3_substep = 0;
                    red_obs_state = RED_OBS_PHASE_4;
                }
            }
            break;
            
        case RED_OBS_PHASE_4:
            // 右转90° + 前进H(30cm)
            static uint8_t phase4_substep = 0;
            
            if(phase4_substep == 0) {
                // 右转90°
                begin_angle_integeral(RED_OBS_TURN_ANGLE);
                motor_set_speed_direct(RED_OBS_TURN_SPEED, -RED_OBS_TURN_SPEED);
                
                if(get_integeral_state(&angle_integral) == 2) {
                    clear_angle_integeral();
                    phase4_substep = 1;
                }
            }
            else if(phase4_substep == 1) {
                // 前进30cm
                begin_distant_integeral(RED_OBS_LATERAL_DIST);
                motor_set_speed_direct(RED_OBS_FORWARD_SPEED, RED_OBS_FORWARD_SPEED);
                
                if(get_integeral_state(&distance_integral) == 2) {
                    clear_distant_integeral();
                    phase4_substep = 0;
                    red_obs_state = RED_OBS_PHASE_5;
                }
            }
            break;
            
        case RED_OBS_PHASE_5:
            // 左转90°（回到与赛道平行）
            begin_angle_integeral(-RED_OBS_TURN_ANGLE);
            motor_set_speed_direct(-RED_OBS_TURN_SPEED, RED_OBS_TURN_SPEED);
            
            if(get_integeral_state(&angle_integral) == 2) {
                clear_angle_integeral();
                red_obs_state = RED_OBS_OUT;
            }
            break;
            
        case RED_OBS_OUT:
            // 避障完成，恢复图像巡线
            red_obs_state = RED_OBS_IDLE;
            // 通知元素识别系统继续
            break;
    }
}
```

---

## 五、运动轨迹图示

```
赛道边界 ┃
        ┃        ↑ (5)左转90°回到平行
        ┃        │
        ┃    ────┘ (4)右转90° + 前进H
        ┃    │
        ┃    │ ← (3)右转90° + 前进L(绕过障碍)
        ┃    │
        ┃    └──── (2)左转90° + 前进H(离开赛道)
        ┃         ↑
        ┃  [障碍] │ (1)直行20cm
        ┃  █████  │
        ┃  █████  ●← 起点（检测到红色障碍）
        ┃
```

**关键说明**：
- **(1)** 直行20cm：确保完全脱离障碍物检测区域
- **(2)** 左转90° + 前进H：横向移动离开赛道
- **(3)** 右转90° + 前进L：沿赛道方向绕过障碍
- **(4)** 右转90° + 前进H：横向回到赛道附近
- **(5)** 左转90°：车身回到与赛道平行，准备巡线

---

## 六、实现要点

### 6.1 精度保证措施

1. **编码器零点校准**：每次启动积分前清零 `clear_distant_integeral()`
2. **陀螺仪漂移补偿**：短时间内陀螺仪积分漂移较小，可忽略（长期需要零偏校准）
3. **速度闭环控制**：使用PID控制保证左右轮速度一致，减少直行偏差
4. **分段累积误差**：每个阶段完成后重新清零积分器，避免误差累积

### 6.2 电机控制接口

需要实现以下电机控制函数：

```c
/**
 * @brief 直接设置左右轮速度（不依赖PID）
 * @param left_speed  左轮速度(cm/s)，负值表示后退
 * @param right_speed 右轮速度(cm/s)，负值表示后退
 */
void motor_set_speed_direct(float left_speed, float right_speed);
```

### 6.3 参数调试建议

| 参数 | 初始值 | 调试方法 |
|------|--------|----------|
| `RED_OBS_FORWARD_DIST` | 20cm | 保证脱离检测区即可 |
| `RED_OBS_LATERAL_DIST` | 30cm | 根据赛道宽度调整 |
| `RED_OBS_BYPASS_DIST` | 40cm | 根据障碍物长度调整 |
| `RED_OBS_TURN_ANGLE` | 90° | 陀螺仪标定后微调 |
| `RED_OBS_FORWARD_SPEED` | 50cm/s | 降低速度提高精度 |

---

## 七、与元素识别系统集成

### 7.1 触发条件

在 `Element_recognition.c` 的 `enter_task()` 中：

```c
if(detect_red_area())  // 由用户实现的红色区域检测
{
    element_now = red_obstacle;
    red_obs_state = RED_OBS_ENTER;  // 触发避障
}
```

### 7.2 避障执行

在 `Element_recognition()` 的 `case red_obstacle:` 中：

```c
case red_obstacle:
    red_obstacle_avoid();  // 调用避障函数
    
    if(red_obs_state == RED_OBS_OUT)  // 避障完成
    {
        element_now = auto_scan;  // 恢复自动扫描
    }
    break;
```

---

## 八、优势与局限

### 优势
✅ **完全独立**：不依赖图像处理，避免光照干扰  
✅ **轨迹可控**：基于数学模型，轨迹可预测  
✅ **参数化设计**：便于根据场地调整  
✅ **实时性强**：无需图像处理延迟

### 局限
⚠️ **累积误差**：长距离运动会有位置偏差（需分段清零缓解）  
⚠️ **场地依赖**：要求赛道平整，编码器打滑会影响精度  
⚠️ **参数敏感**：需要精确标定编码器、陀螺仪参数  
⚠️ **无反馈修正**：无法检测偏离，建议在最后阶段用图像微调

---

## 九、进阶优化方向

1. **混合导航**：避障最后阶段重新启用摄像头，用边线检测微调回赛道
2. **自适应参数**：根据速度动态调整转向角度补偿
3. **路径平滑**：使用贝塞尔曲线代替直角转弯，提高平稳性
4. **失败恢复**：添加超时机制，超时后强制恢复图像巡线

---

## 十、总结

该方案通过**航位推算**实现了完全脱离图像的红色障碍避障，核心思路是**分段执行固定动作序列**，利用编码器和陀螺仪的积分器完成精确的距离和角度控制。实际应用中需要：

1. **精确标定**编码器和陀螺仪参数
2. **调试参数**适应实际场地尺寸
3. **分段清零**积分器减少误差累积
4. **低速运行**提高控制精度

该方案适合在图像处理受限（光照不足、颜色干扰）的场景下使用。
